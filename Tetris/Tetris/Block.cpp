
#include "Block.h"

/*
const Vector2 Block::START_BLOCK_CENTER_POINT = 
{ 
    static_cast<float>(Engine::GetInstance()->GetWindowResolution().x / 2), 
    static_cast<float>(Engine::GetInstance()->GetWindowResolution().y / 2) 
};
*/

/*
const Vector2 Block::BLOCK_START_POINT =
{
    0.f, 0.f
};

const float Block::BLOCK_WIDTH = 30.f;
const float Block::BLOCK_HEIGHT = 30.f;

Block::Block()
    : GraphicsObject(BLOCK_START_POINT, BLOCK_WIDTH, BLOCK_HEIGHT)
{
}
*/